[tool.poetry]
name = "openai-slack"
version = "0.1.0"
description = ""
authors = [""]

[tool.poetry.dependencies]
python = "^3.8"
feedparser = "^6.0.10"
openai = "^0.27.2"

[tool.poetry.dev-dependencies]